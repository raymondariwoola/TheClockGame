// Chronos Strike — Cloudflare backend configuration.

window.CHRONOS_LB_CONFIG = {
  // Deployed Worker base URL. Keep secrets in Worker secret bindings only.
  apiBase: 'https://chronos-leaderboard.raymondariwoola.workers.dev',
};
